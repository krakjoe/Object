// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("object", "for object support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("object", "enable object support", "no");

if (PHP_OBJECT != "no") {
	EXTENSION("object", "object.c");
}

